TEX := haowang_cv_zh.tex
PDF := haowang_cv_zh.pdf
PUBLISHED_PDF := ../haowang_cv_zh.pdf

.PHONY: all publish clean

all:
	latexmk -xelatex -interaction=nonstopmode -halt-on-error $(TEX)

publish: all
	cp $(PDF) $(PUBLISHED_PDF)

clean:
	latexmk -c -xelatex $(TEX)
	rm -f *.synctex.gz
